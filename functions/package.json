{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "webpack && firebase deploy --only functions",
    "deploy-prod": "webpack --optimize-minimize && firebase deploy --only functions"
  },
  "dependencies": {
    "cors": "^2.8.4",
    "firebase-admin": "~4.2.1",
    "firebase-functions": "^0.5.9"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^3.5.0",
    "ts-loader": "^2.3.4",
    "typescript": "^2.4.2",
    "webpack-node-externals": "^1.6.0"
  }
}
